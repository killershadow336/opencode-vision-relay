{
  "name": "opencode-vision-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Native OpenCode V2 plugin that gives text-only models vision: relays image parts to Gemini (OpenAI-compatible endpoint) and replaces them with detailed text analyses.",
  "exports": "./index.ts",
  "main": "./index.ts",
  "files": [
    "index.ts",
    "options.ts",
    "images.ts",
    "gemini.ts",
    "relay.ts",
    "live.ts",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@opencode-ai/ai": "0.0.0-next-17444",
    "@opencode-ai/plugin": "0.0.0-next-17444"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.0",
    "vitest": "^3.0.0"
  }
}
